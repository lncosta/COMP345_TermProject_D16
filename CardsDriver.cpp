#include "Cards.h"

int main()
{
	Card c1;
	// c1.cardType = BOMB;
	c1.play();

	Deck d1;
	// printDeck(d1);


	// Create a deck 
	// Later on the deck will contain 3 times each type of card per player. Ex. 4 players would be 4 players * 5 types of cards * 3 card per type = 60 cards


	// Create a hand and draw a few cards to fill it

	// Call the play() method of a few cards in the hand
};